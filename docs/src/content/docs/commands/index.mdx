---
title: "コマンド一覧"
description: "esa-cliのコマンド一覧と学習順序"
---

import { Aside } from '@astrojs/starlight/components';

esa-cliのコマンド一覧です。学習順序に従って段階的にコマンドを学ぶことができます。

## 学習順序

初めての方には以下の順序で学習することをお勧めします：

1. **[初期設定](/esa-cli/commands/setup)** - 最初に実行する必要があります
2. **[記事一覧](/esa-cli/commands/list)** - 記事の確認から始めましょう
3. **[記事作成](/esa-cli/commands/create)** - 新しい記事を作成しましょう
4. **[記事取得](/esa-cli/commands/fetch)** - 記事のダウンロードを学びましょう
5. **[記事更新](/esa-cli/commands/update)** - 記事の更新を学びましょう
6. **[記事一括移動](/esa-cli/commands/move)** - 一括処理を学びましょう

<Aside type="tip" title="段階的な学習">
各コマンドは段階的に学習できるよう構成されています。基本的なコマンド（1-5）をマスターしてから高度なコマンド（6）に進むことをお勧めします。
</Aside>

## コマンド一覧

<Aside type="tip" title="AIエディタ向けガイド">
AIエディタでコマンドを選択する際は、[AIエディタ向けガイド](/esa-cli/guides/ai-editor-guide)を参照してください。タスクベースのコマンド選択や判断フローが記載されています。
</Aside>

### 基本コマンド

| コマンド | 説明 | 詳細 |
|---------|------|------|
| `setup` | 初期設定 | [詳細を見る](/esa-cli/commands/setup) |
| `list` | 記事一覧表示 | [詳細を見る](/esa-cli/commands/list) |
| `create` | 記事作成 | [詳細を見る](/esa-cli/commands/create) |
| `fetch` | 記事ダウンロード | [詳細を見る](/esa-cli/commands/fetch) |
| `update` | 記事更新 | [詳細を見る](/esa-cli/commands/update) |

### 高度なコマンド

| コマンド | 説明 | 詳細 |
|---------|------|------|
| `move` | 記事一括移動 | [詳細を見る](/esa-cli/commands/move) |
| `fetch-all` | 記事一括ダウンロード | 下記参照 |
| `update-all` | 記事一括更新 | 下記参照 |

### 一括処理コマンド

#### `fetch-all`

条件に一致する記事を一括でダウンロードします。

```bash
esa-cli fetch-all [オプション]
```

**主なオプション:**
- `--category` - カテゴリでフィルタ
- `--tag` - タグでフィルタ
- `--query` - 検索ワードでフィルタ
- `--user` - 作成者でフィルタ
- `--limit` - 取得件数制限（デフォルト: 10）

**使用例:**
```bash
# 特定のカテゴリの記事を一括ダウンロード
esa-cli fetch-all --category 開発

# 特定のタグの記事を一括ダウンロード
esa-cli fetch-all --tag API

# 取得件数を制限して一括ダウンロード
esa-cli fetch-all --category 開発 --limit 5
```

#### `update-all`

複数の記事を一括で更新します。

```bash
esa-cli update-all [パターン] [オプション]
```

**主なオプション:**
- `pattern` - ファイルパターン（例: "123-*.md"、デフォルト: "*.md"）
- `--message` - 更新メッセージ
- `--no-wip` - WIP状態を解除
- `--category` - カテゴリを変更
- `--force` - 確認なしで実行

**使用例:**
```bash
# 現在のディレクトリの全記事を一括更新
esa-cli update-all

# 特定のパターンの記事を一括更新
esa-cli update-all "123-*.md"

# 確認なしで一括更新
esa-cli update-all --force
```

<Aside type="tip" title="実践的な使い方">
実践的な使用例については[基本的な使い方](/esa-cli/guides/basic-usage)と[高度な使い方](/esa-cli/guides/advanced-usage)を参照してください。
</Aside>

## 設定ファイル

設定ファイルは `~/.esa-cli-config.json` に保存されます。

### 設定項目

```json
{
  "team_name": "your-team-name",
  "access_token": "your-token"
}
```

#### 設定項目の説明

- `team_name` - esaのチーム名（サブドメイン）
- `access_token` - esaのアクセストークン

## 環境変数

以下の環境変数を使用できます：

- `ESA_TEAM` - チーム名
- `ESA_TOKEN` - アクセストークン

## エラーコード

- `E001` - 設定ファイルが見つかりません
- `E002` - アクセストークンが無効です
- `E003` - 記事が見つかりません
- `E004` - ファイルの形式が不正です
- `E005` - ネットワークエラー